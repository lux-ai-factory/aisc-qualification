[pytest]
testpaths = tests
pythonpath = .
# rdflib json-ld parser builds a ConjunctiveGraph internally; that warning is theirs.
filterwarnings =
    ignore:ConjunctiveGraph is deprecated:DeprecationWarning
